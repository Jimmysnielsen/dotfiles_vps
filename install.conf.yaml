- defaults:
    link:
      relink: true
- clean: ['~']
- link:
    ~/.bashrc: bashrc
    ~/.zshrc: zshrc
    ~/.emacs: emacs
    ~/.emacs.d: emacs.d
    ~/howm: /home/root/howm
    #~/.tmux.conf: tmux.conf
#- create:
- shell:
  - [git submodule update --init --recursive, Installing submodules]
