- defaults:
    link:
      relink: true
- clean: ['~']
- link:
    ~/.bashrc: bashrc
    ~/.zshrc: zshrc
    ~/.tmux.conf: tmux.conf
#- create:
- shell:
  - [git submodule update --init --recursive, Installing submodules]
